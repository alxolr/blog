import TimeAgo from 'javascript-time-ago'
import en from 'javascript-time-ago/locale/en'
$ TimeAgo.locale(en);
$ var timeAgo = new TimeAgo('en-US');
<p.blog-post-meta>
  Added ${timeAgo.format(input.article.createdAt)} - <i>${input.article.readTime} min read</i>
</p>
